# 대학알리미 데이터 분석 웹앱 제작 프롬프트

아래 프롬프트는 대학알리미 데이터를 수집하고, 정제하고, 대시보드로 제작하는 웹앱을 단계적으로 구현하기 위한 통합 개발 지시서다. 각 단계는 이전 단계의 결과를 유지하면서 순서대로 진행한다.

## 공통 개발 원칙

- 프런트엔드는 반응형 웹으로 구현하고 한국어 UI를 기본으로 한다.
- 백엔드는 Node.js, 데이터베이스는 PostgreSQL, 원본·정제·대시보드 파일은 파일 Bucket에 저장한다.
- 장시간 작업은 비동기로 처리하고 진행률, 성공, 실패, 오류 원인, 재시도 상태를 실시간으로 표시한다.
- API 키와 비밀값은 소스 코드나 생성 HTML에 저장하지 않는다.
- 파일은 이름만 믿지 말고 확장자, MIME 유형, 파일 시그니처, 실제 크기를 검증한다.
- 모든 수정 시 `v.1.0.0` 형식의 버전을 올리고 테스트 후 GitHub에 커밋·푸시한다.
- 기존 기능과 사용자 데이터를 보존하고, 실패한 항목만 안전하게 다시 처리할 수 있게 한다.

---

## 1단계 — 기반 시스템과 데이터 수집

```text
대학알리미 공시자료를 안정적으로 수집하는 웹앱의 1단계를 구현해줘.

요구사항:
1. Node.js 서버, PostgreSQL, 파일 Bucket을 사용하는 구조로 구성한다.
2. 공시 항목 조회 결과에 번호, 학교 종류, 분류, 실제 다운로드 파일명, 선택 연도, 처리 상태, 개별 다운로드 버튼을 표시한다.
3. 2026년 자료를 우선 조회하고, 없는 항목만 2025년 자료로 대체한다.
4. 전체 다운로드와 개별 다운로드를 모두 지원하며 이미 받은 파일도 다시 실행하면 재다운로드하고 안전하게 덮어쓴다.
5. ZIP 응답, HTML 오류 페이지, 빈 파일을 XLSX로 오인하지 않도록 응답 헤더와 파일 시그니처를 검증한다.
6. 중복 항목과 동일 파일을 식별해 화면의 대상 수와 실제 고유 파일 수를 구분해 표시한다.
7. 조회·다운로드 중에는 움직이는 시계 아이콘, 누적 처리 수, 전체 파일 수, 성공 수, 실패 수를 표시한다.
8. 실패 항목에는 원인과 재시도 버튼을 제공한다. 재시도는 해당 항목만 처리한다.
9. 사용자가 선택한 1단계 폴더를 이후 단계의 기준 폴더로 저장한다.
10. 각 단계의 처리 현황 패널은 페이지 맨 아래가 아니라 해당 단계 바로 아래에 표시한다.

완료 조건:
- 전체 대상과 성공·실패·중복 수가 일관되게 집계된다.
- 저장된 모든 XLSX가 실제로 열리는 유효한 파일이다.
- 실패한 개별 항목만 재다운로드할 수 있다.
```

---

## 2단계 — Excel 데이터 정제 엔진

```text
1단계에서 수집한 대학알리미 XLSX 파일을 정확하게 정제하는 Node.js 엔진과 UI를 구현해줘.

요구사항:
1. 1단계 기준 폴더 아래에 '정제' 폴더를 자동 생성하고, 사용자가 별도 폴더를 선택하면 그 위치를 우선한다.
2. 폴더 일괄 처리와 개별 XLSX 업로드 후 수동 정제를 모두 지원한다.
3. XLSX의 ZIP 시그니처, 워크북 구조, 시트 존재 여부, 실제 데이터 범위를 검증한 뒤 파싱한다.
4. 상단 제목·주석·공백 행을 제거하되 실제 표 헤더를 자동 탐지한다. 병합 셀과 다중 행 헤더는 의미가 보존되도록 평탄화한다.
5. 학교명, 본분교, 학교 종류, 설립 구분, 지역, 주소 등 식별 열은 보존하고 동일 캠퍼스 행은 공시 항목별 규칙에 따라 통합한다.
6. 인원·금액·비율 열을 구분하고 쉼표, %, 괄호 음수, 빈 값, 하이픈을 안전하게 숫자로 변환한다.
7. 취업률처럼 수식명이 열에 포함된 경우 수식 문자열을 값으로 오인하지 않는다. 취업률은 공식 분모·분자 열을 확인해 재계산하고 원본 제공값과 교차 검증한다.
8. 외국학생, 전임교원, 연구비 등 서로 다른 공시 양식을 파일명과 헤더 패턴으로 판별하고 항목별 정제 규칙을 적용한다.
9. 데이터 행과 헤더를 찾지 못하면 임의로 0행 결과를 만들지 말고 실패 원인과 탐지 후보를 기록한다.
10. 항목별 파일명, 정제 상태, 원본 행 수, 결과 행 수, 통합 행 수, 오류 내용, '재정제' 버튼을 표시한다.

검증 조건:
- 정제 전후의 학교 수와 핵심 합계가 허용 범위 안에서 일치한다.
- 취업률 등 계산 지표는 표본 행을 공식 산식으로 재계산해 검증한다.
- 빈 파일과 손상 파일은 결과물로 저장하지 않는다.
- 실패 항목만 개별 재정제할 수 있다.
```

---

## 3단계 — 데이터 대시보드 제작

```text
정제된 XLSX마다 독립 실행 가능한 반응형 HTML 데이터 대시보드를 생성해줘.

요구사항:
1. 1단계 기준 폴더 아래에 '대시보드' 폴더를 자동 생성하고, 개별 XLSX 업로드 후 수동 제작도 지원한다.
2. 데이터 구조에 맞춰 KPI 카드, 순위, 막대·선·분포·구성비·산점도 등 유효한 차트를 가능한 범위에서 생성한다.
3. 차트에는 단위, 기준연도, 표본 수, 결측치 처리 기준을 명시하고 잘못된 합계나 평균을 만들지 않는다.
4. 전국 대학 분포 지도는 Google Maps로 구현한다. 설정에 입력한 Google Maps API 키를 서버를 거쳐 사용하되 생성 HTML에 키를 평문 저장하지 않는다.
5. 대학 주소를 우선 사용해 정확한 좌표를 매칭하고, 매칭되지 않은 대학은 지역 대표 좌표에 임의 배치하지 말고 미확인으로 기록한다.
6. 지도에는 데이터에 포함된 모든 대학을 표시한다. 팝업에는 대학명, 학교 종류, 설립 구분, 지역, 주소와 핵심 결과값만 표시하고 수식 문자열은 제외한다.
7. 'OpenAI 텍스트 분석'이라는 명칭은 'AI 분석'으로 표시한다. 핵심 인사이트는 데이터에 근거한 서술형 요약과 핵심 지표 태그로 구성한다.
8. AI 분석에는 전체 규모, 대학별 격차, 설립 유형, 지역 집중, 성별 또는 집단 차이, 주의할 이상치와 기준연도를 포함하되 데이터에 없는 사실은 생성하지 않는다.
9. OpenAI API 호출이 실패하거나 키가 없으면 통계 기반 규칙형 요약으로 대체하여 대시보드 생성 자체는 중단하지 않는다.
10. 항목별 파일명, 제작 상태, 결과 HTML, 오류 내용, '재제작' 버튼을 표시한다.
11. 모든 HTML 하단에 다음 푸터를 넣는다.

Copyright 2026 ⓒ All Rights Reserved. | 박정원(한국외국어대학교)
AI 뉴스레터 빌더: https://kletter.kr/ · AI 강의콘텐츠 빌더: https://kteacher.kr/
본 대시보드는 AI 분석 참고용이며, 공식 통계 수치와 차이가 있을 수 있습니다.

완료 조건:
- 네트워크가 일시적으로 실패해도 지도 또는 AI 분석 때문에 전체 HTML 생성이 실패하지 않는다.
- 모든 차트와 인사이트가 실제 정제 데이터에서 추적 가능하다.
- 실패 항목만 개별 재제작할 수 있다.
```

---

## 4단계 — 대학 경쟁력 통합 마스터 데이터

```text
정제 폴더의 공시자료를 대학 단위로 통합해 한국 대학의 경쟁력, 운영 내실, 교육·연구 성과, 국제화, 평판 대리지표를 비교할 수 있는 마스터 데이터를 만들어줘.

요구사항:
1. 대학명 표준화 사전과 캠퍼스·본분교 식별키를 만들고 동명이교나 명칭 변경을 안전하게 처리한다.
2. 대학별 한 행 구조로 통합하되 원본 항목, 기준연도, 산식, 단위, 결측 사유를 추적할 수 있게 한다.
3. 교육 경쟁력: 학생 1인당 교육비, 전임교원 확보율, 소규모 강좌 비율, 중도탈락률, 취업률 등을 포함한다.
4. 연구 경쟁력: 전임교원 1인당 연구비, 논문, 특허, 기술이전 수입과 계약 실적 등을 포함한다.
5. 국제화: 외국인 학생 비율, 학위과정 비율, 해외 교류 관련 지표를 포함한다.
6. 운영 내실: 등록금 의존도, 운영수지, 부채 비율, 법인전입금, 장학금, 교육비 환원 관련 지표를 포함한다.
7. 평판도는 직접 측정 자료가 없으면 취업·연구·충원·국제화 등의 객관적 대리지표임을 명시하고 주관적 순위를 만들지 않는다.
8. 원시값과 표준화 점수를 함께 제공한다. 결측값을 0으로 간주하지 말고 지표별 유효 표본 수를 표시한다.
9. 극단값 처리, 방향성, 표준화 방식, 가중치를 '지표정의' 시트에 기록한다.
10. '대학별_마스터', '지표정의', '데이터품질', '요약' 시트를 포함한 XLSX와 통합 대시보드를 생성한다.

완료 조건:
- 대학별 원본 값에서 마스터 지표까지 역추적할 수 있다.
- 수식 오류, 잘못된 0값, 중복 대학 행이 없다.
- 종합점수는 구성 지표와 가중치를 공개하고 데이터 완전성이 낮은 대학에 경고를 표시한다.
```

---

## 5단계 — 운영 센터, 보안, 배포 및 품질 보증

```text
완성된 웹앱을 Railway와 GitHub Pages에서 안정적으로 운영할 수 있도록 운영 기능과 배포 체계를 완성해줘.

요구사항:
1. Railway에는 Node.js API, PostgreSQL, 파일 Bucket 연결 정보를 환경변수로 설정하고 시작 시 필수 환경변수를 검증한다.
2. 사용자별 OpenAI API 키와 Google Maps API 키는 암호화하거나 세션 범위로 관리하며 로그, DB 평문, 정적 HTML, Git 저장소에 노출하지 않는다.
3. GitHub Pages는 정적 프런트엔드로 배포하고 Railway API 주소를 환경별 설정으로 분리한다. API 응답 전에 HTML 오류 페이지가 반환되는 상황을 감지한다.
4. 운영 센터에 동일 주제의 1단계 원본 XLSX, 2단계 정제 XLSX, 3단계 대시보드 HTML 예제를 각각 하나씩 제공한다.
5. 예제 다운로드는 실제 파일 존재, MIME 유형, Content-Disposition, CORS를 자동 테스트한다.
6. 작업 이력, 처리 시간, 성공·실패 수, 재시도 횟수, 오류 코드, 파일 체크섬을 PostgreSQL에 기록한다.
7. 서버 재시작 후에도 진행 상태를 복구하고 중복 작업을 방지하는 작업 큐와 잠금 정책을 적용한다.
8. 대용량 파일과 다수 항목은 동시 처리 수를 제한하고 지수 백오프 재시도를 적용한다.
9. 수집·정제·대시보드 각 단계에 단위 테스트, 통합 테스트, 대표 XLSX 회귀 테스트를 추가한다.
10. 접근성, 모바일 화면, 키보드 조작, 오류 메시지, 로딩 애니메이션을 점검한다.
11. 제목 아래에 `v.1.0.0` 형식의 버전을 빨간색으로 표시하고 수정할 때마다 SemVer 기준으로 증가시킨다.
12. 구현과 검증이 끝나면 변경 파일만 선택하여 명확한 메시지로 GitHub main 브랜치에 커밋하고 푸시한다.

최종 완료 조건:
- 1단계 수집 → 2단계 정제 → 3단계 대시보드가 신규 사용자 환경에서 연속 수행된다.
- 개별 업로드와 실패 항목 재작업이 정상 작동한다.
- 데이터 정확성 검증 보고서와 배포 점검 결과가 함께 제공된다.
- 비밀키가 저장소와 생성 결과물에 포함되지 않는다.
```

## 권장 실행 방법

각 단계의 프롬프트를 한 번에 하나씩 개발 에이전트에 전달한다. 다음 단계로 넘어가기 전에 해당 단계의 완료 조건을 테스트하고, 발견된 오류는 같은 단계 안에서 수정·재검증한다. 5단계가 끝나면 전체 회귀 테스트를 수행한다.
